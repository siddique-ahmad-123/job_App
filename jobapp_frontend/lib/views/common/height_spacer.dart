import 'package:flutter/material.dart';

class HeightSpacer extends StatelessWidget {
  const HeightSpacer({super.key, required int size});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}